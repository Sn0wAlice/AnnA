export class Services {
    main(servicesList:any, client:any, data:any){
        if(data.service.name == "create"){
            //create a new service
        } else if(data.service.name == "delete"){
            //delete a service
        } else if(data.service.name == "join"){
            //join a service
        }
    }
}